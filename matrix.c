#include<stdio.h>
int main()
{
     int a,b,c;
     int *ptr;
*ptr=NULL;
     printf("enter\n");
  printf("%x or %d ",*ptr,*ptr);
     for(a=0;a<4;a++)
     {
         for(b=0;b<5;b++)
         {
             printf("%d",b);
         } printf("%d\n",a);
     } return 0 ;
}